<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.position.positionSquareService.mapper.RoleMapper" >
    <resultMap id="roleMap" type="map" >
        <result property="roleName" column="ROLE_NAME"/>
        <result property="id" column="ID" />
    </resultMap>
    
    <resultMap id="roleUserMap" type="map" >
        <result  property="roleName" column="ROLE_NAME"/>
        <collection property="acls" ofType="java.lang.String" javaType="list">
			<result column="ACL_NAME" javaType="java.lang.String" />
		</collection>
    </resultMap>
  
    <select id="getRoleMaster" resultMap="roleMap"  >
       SELECT ROLE_NAME,ID FROM ROLE_MASTER where IS_INTERNAL!=1;
    </select>
    
    <select id="getRoleForUser" resultMap="roleUserMap"  >
       SELECT rm.ROLE_NAME , ra.ACL_NAME as ACL_NAME FROM ROLES r
       LEFT JOIN ROLE_MASTER rm ON rm.ID=r.ROLE_ID 
       LEFT JOIN ROLE_ACLS ra ON ra.ROLE_ID = rm.ID 
       where r.USER_ID = #{userId} 
    </select>

   
</mapper>